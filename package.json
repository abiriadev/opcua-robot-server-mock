{
	"name": "opcua-robot-server-mock",
	"version": "0.1.0",
	"description": "OPC UA server mock that emulates behavior of a robot arm",
	"scripts": {
		"build": "turbo build",
		"clean": "turbo clean",
		"format": "prettier ./package.json ./turbo.json ./pnpm-workspace.yaml -w",
		"format:all": "turbo format",
		"lint": "turbo lint",
		"all": "turbo clean format lint build start",
		"prune": "turbo prune --scope server --docker"
	},
	"keywords": [
		"OPC",
		"opcua",
		"mock",
		"emulator",
		"robotics"
	],
	"author": "Abiria <abiria.dev@gmail.com>",
	"license": "GPL-3.0",
	"devDependencies": {
		"prettier": "^2.8.2",
		"turbo": "^1.7.0"
	}
}
